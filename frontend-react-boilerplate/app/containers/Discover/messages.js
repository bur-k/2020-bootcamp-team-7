/*
 * Discover Messages
 *
 * This contains all the text for the Discover container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Discover';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Discover container!',
  },
});
